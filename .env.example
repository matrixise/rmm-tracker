# Configuration de la base de données PostgreSQL (requis)
DATABASE_URL=postgres://user:pass@localhost:5432/rmm_tracker?sslmode=disable

# Override de la configuration (optionnel, prioritaire sur config.toml)
# Format recommandé avec préfixe RMM_TRACKER_

# Multiple RPC endpoints (comma-separated, recommended)
# RMM_TRACKER_RPC_URLS=https://rpc.gnosischain.com,https://gnosis.drpc.org,https://rpc.ankr.com/gnosis

# Or single endpoint (legacy, still supported)
# RMM_TRACKER_RPC_URL=https://rpc.gnosischain.com

# RMM_TRACKER_WALLETS=0xAddress1,0xAddress2,0xAddress3
# RMM_TRACKER_LOG_LEVEL=info
# RMM_TRACKER_INTERVAL=5m

# Format legacy (toujours supporté pour compatibilité)
# RPC_URL=https://rpc.gnosischain.com
# RPC_URLS=https://rpc1.com,https://rpc2.com
# WALLETS=0xAddress1,0xAddress2,0xAddress3
# LOG_LEVEL=info

# Niveau de logs (optionnel, défaut: info)
# Valeurs possibles: debug, info, warn, error
# RMM_TRACKER_LOG_LEVEL=info

# Intervalle d'exécution en mode daemon (optionnel)
# Option 1: Duration (converti automatiquement en cron aligné sur l'horloge)
# Exemples: 5m, 10m, 15m, 30m, 1h, 2h
# RMM_TRACKER_INTERVAL=5m

# Option 2: Expression cron pour planification avancée
# RMM_TRACKER_INTERVAL="*/5 * * * *"      # Toutes les 5 minutes
# RMM_TRACKER_INTERVAL="0 */2 * * *"      # Toutes les 2 heures à :00
# RMM_TRACKER_INTERVAL="0 9,17 * * 1-5"   # 9h et 17h en semaine

# Options du scheduler
# RMM_TRACKER_RUN_IMMEDIATELY=true        # Exécuter immédiatement au démarrage (défaut: true)
# RMM_TRACKER_TIMEZONE=UTC                # Fuseau horaire (défaut: UTC)
# RMM_TRACKER_TIMEZONE=America/New_York   # Exemple: Heure de New York
